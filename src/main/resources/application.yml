#??????
spring:
  datasource:
    url: jdbc:mysql://localhost:5100/pfc
    username: root
    password: ${password:password}
    driver-class-name: com.mysql.cj.jdbc.Driver
    jackson:
      serialization:
        fail-on-empty-beans: false
